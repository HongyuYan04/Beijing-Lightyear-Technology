server:
  port: 8888

spring:
  # 配置数据源
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/Human Resources Management System?useUnicode=true&characterEncoding=utf8&serverTimezone=GMT%2b8
    username: root
    password: WeChat666
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.zaxxer.hikari.HikariDataSource

  mvc:
    path match:
      matching-strategy: ant-path-matcher

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  # 扫描通用枚举包
  type-enums-package: com.cow.enums

file:
  upload:
    path: E:\Java Program\JP\Program\files